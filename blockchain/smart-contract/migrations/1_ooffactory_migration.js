const oofFactory = artifacts.require("OOFFactory");

module.exports = async function (deployer, network, accounts) {
  // deployment steps
  factoryOwner = "";
  oofImplementation = "";
  conjureRouter = "";
  await deployer.deploy(oofFactory, );
};